name: color_adjust
description: An extension on the Flutter Color class that provides color manipulation methods.

repository: https://github.com/edunnett/color_adjust
version: 1.0.5

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

screenshots:
  - description: "This screenshot shows the example."
    path: assets/screenshot.png
